{
  "description": "Multivariate Gaussian, d=5, diagonal covariance",
  "target_type": "gaussian",
  "dimension": 5,
  "parameters": ["p1", "p2", "p3", "p4", "p5"],
  "prior": "uniform, wide enough to be non-informative",
  "posterior_mean": [2.0, -1.0, 0.5, 3.0, -2.0],
  "posterior_variance": [1.0, 4.0, 0.25, 2.0, 1.0],
  "posterior_std": [1.0, 2.0, 0.5, 1.414, 1.0],
  "notes": "With direct_pass objective and uniform priors, the posterior is N(mu, Sigma) where Sigma = diag(variance). The NLL is 0.5 * sum((x-mu)^2 / sigma^2), so lnlikelihood = -NLL gives the correct Gaussian log-density."
}
